<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Siswa - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes popIn {
            from { opacity: 0; transform: scale(0.95) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        .animate-pop { animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }

        #splash {
            position: fixed; inset: 0; background: #ffffff; z-index: 100;
            display: flex; flex-direction: column; align-items: center; justify-content: center;
            transition: opacity 0.8s ease-out, visibility 0.8s;
        }
        .loader-bar { width: 150px; height: 3px; background: #e2e8f0; position: relative; overflow: hidden; margin-top: 20px; border-radius: 10px; }
        .loader-progress { position: absolute; height: 100%; width: 50%; background: #0ea5e9; animation: loading 1.5s infinite ease-in-out; border-radius: 10px; }
        @keyframes loading { from { left: -50%; } to { left: 100%; } }

        body { background-color: #f0f9ff; }
    </style>
</head>
<body class="text-slate-800 flex items-center justify-center min-h-screen p-4 py-10 overflow-hidden font-sans">

    <div id="splash">
        <div class="text-6xl mb-4 animate-bounce">🎓</div>
        <h2 class="text-sm font-bold tracking-[0.3em] uppercase text-sky-500">Student Portal</h2>
        <div class="loader-bar"><div class="loader-progress"></div></div>
    </div>

    <div class="animate-pop bg-white p-8 md:p-12 rounded-[30px] w-full max-w-[450px] shadow-2xl shadow-sky-100 border border-slate-100 relative z-10">
        
        <div class="flex justify-between items-start mb-8">
            <div>
                <h1 class="text-3xl font-bold text-slate-800 tracking-tight">Login Siswa</h1>
                <p class="text-xs text-slate-400 font-semibold uppercase tracking-wider mt-2">Siap belajar hari ini?</p>
            </div>
            <div class="p-3 bg-sky-50 rounded-xl text-2xl">✏️</div>
        </div>

        <% if (locals.msg) { %>
            <div class="bg-red-50 text-red-500 p-4 rounded-xl mb-6 text-center font-bold border border-red-100 text-xs shadow-sm">
                <%= msg %>
            </div>
        <% } %>

        <form action="/auth/login-siswa" method="POST" class="space-y-5">
            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">Email Siswa</label>
                <input name="email" type="email" placeholder="siswa@sekolah.com" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-sky-400 focus:bg-white focus:ring-2 focus:ring-sky-100 text-lg transition-all placeholder:text-slate-300 text-slate-700 font-medium" required>
            </div>

            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">Password</label>
                <input name="pass" type="password" placeholder="••••••••" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-sky-400 focus:bg-white focus:ring-2 focus:ring-sky-100 text-lg transition-all placeholder:text-slate-300 text-slate-700 font-medium" required>
            </div>

            <button type="submit" 
                class="w-full bg-sky-500 py-4 rounded-xl font-bold text-lg hover:bg-sky-600 transition-all shadow-lg shadow-sky-200 mt-2 text-white active:scale-95">
                Mulai Belajar
            </button>
        </form>

        <div class="mt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-[11px] font-bold text-slate-400 tracking-wider uppercase">
            <a href="/register-siswa" class="hover:text-sky-500 transition-colors">Belum Punya Akun?</a>
            <a href="/forget-siswa" class="hover:text-sky-500 transition-colors">Lupa Password?</a>
        </div>

        <div class="mt-8 pt-6 border-t border-slate-100 text-center">
            <a href="/" class="text-[10px] font-bold text-slate-400 hover:text-sky-500 transition-colors uppercase tracking-widest flex items-center justify-center gap-2">
                <span>⬅</span> Kembali ke Portal
            </a>
        </div>
    </div>

    <script>
        window.addEventListener('load', () => {
            setTimeout(() => {
                const splash = document.getElementById('splash');
                splash.style.opacity = '0';
                splash.style.visibility = 'hidden';
                document.body.style.overflow = 'auto';
            }, 1500);
        });
    </script>
</body>
</html>
