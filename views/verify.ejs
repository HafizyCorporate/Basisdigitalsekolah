<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verifikasi OTP - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes popIn {
            from { opacity: 0; transform: scale(0.95) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        .animate-pop { animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }
        body { background-color: #f8fafc; }
    </style>
</head>
<body class="text-slate-800 min-h-screen flex items-center justify-center p-6">

    <div class="max-w-md w-full bg-white p-10 rounded-[30px] shadow-2xl shadow-blue-100 border border-slate-100 animate-pop">
        
        <div class="flex justify-center mb-6">
            <div class="w-20 h-20 bg-blue-50 rounded-full flex items-center justify-center shadow-inner text-4xl text-blue-600">
                🔐
            </div>
        </div>

        <h2 class="text-2xl font-bold text-center mb-2 text-slate-800">Verifikasi OTP</h2>
        <p class="text-slate-400 text-center text-xs uppercase tracking-widest mb-8 font-semibold">Cek Inbox Email Admin</p>

        <% if (msg) { %>
            <div class="bg-blue-50 text-blue-600 p-4 rounded-xl mb-6 text-xs text-center font-bold border border-blue-100">
                <%= msg %>
            </div>
        <% } %>

        <form action="/auth/verify" method="POST" class="space-y-5">
            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">Kode Instansi</label>
                <input name="kode" type="text" placeholder="SCH-XXXXX" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-blue-500 focus:bg-white text-center font-bold text-xl text-slate-700 transition-all placeholder:text-slate-300" required>
            </div>

            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">6-Digit OTP</label>
                <input name="otp" type="text" maxlength="6" placeholder="000000" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-blue-500 focus:bg-white text-center text-3xl tracking-[0.5em] font-bold text-slate-800 transition-all placeholder:text-slate-200" required>
            </div>

            <button type="submit" 
                class="w-full bg-blue-600 py-4 rounded-xl font-bold text-lg hover:bg-blue-700 transition-all shadow-lg shadow-blue-200 mt-4 text-white active:scale-95 uppercase tracking-wide">
                Konfirmasi Akun
            </button>
        </form>

        <div class="mt-10 pt-6 border-t border-slate-100 text-center">
            <p class="text-[10px] text-slate-400 uppercase font-bold tracking-widest mb-3">Tidak menerima kode?</p>
            <a href="/register" class="text-blue-600 hover:text-blue-800 transition-colors text-xs font-bold uppercase tracking-widest">
                Daftar Ulang
            </a>
        </div>
    </div>
</body>
</html>
