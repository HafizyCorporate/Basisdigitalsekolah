<!DOCTYPE html>
<html>
<head><script src="https://cdn.tailwindcss.com"></script></head>
<body class="bg-[#050505] text-white flex items-center justify-center min-h-screen">
    <div class="bg-white/5 border border-white/10 p-12 rounded-[40px] w-[450px] text-center shadow-2xl shadow-emerald-900/20">
        
        <h1 class="text-4xl font-black text-emerald-500 mb-6">RESET AKUN</h1>
        
        <% if (msg) { %>
            <p class="bg-emerald-500/20 text-emerald-200 p-4 rounded-xl mb-6 border border-emerald-500/50 text-sm text-center font-bold">
                <%= msg %>
            </p>
        <% } %>

        <p class="text-gray-400 text-base mb-8">
            Instruksi pemulihan akan dikirim dari <span class="text-emerald-400">azhardax94@gmail.com</span> ke email kamu.
        </p>
        
        <form action="/auth/forget-siswa" method="POST" class="space-y-6">
            <input name="email" type="email" placeholder="EMAIL SISWA" 
                class="w-full bg-black/50 border border-white/10 p-6 rounded-2xl outline-none focus:border-emerald-500 text-xl text-center" required>
            
            <button class="w-full bg-emerald-600 py-6 rounded-2xl font-black text-2xl hover:bg-emerald-500 transition-all shadow-lg shadow-emerald-600/20">
                KIRIM EMAIL
            </button>
        </form>
        
        <a href="/login-siswa" class="block mt-8 text-sm text-gray-500 font-bold tracking-widest uppercase hover:text-white transition-colors">
            ⬅ Kembali Ke Login
        </a>
    </div>
</body>
</html>
