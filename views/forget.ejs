<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pulihkan Akses - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Animasi Masuk */
        @keyframes popIn {
            from { opacity: 0; transform: scale(0.9) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        .animate-pop { animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }

        /* Splash Screen Layer */
        #splash {
            position: fixed;
            inset: 0;
            background: #050505;
            z-index: 100;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: opacity 0.8s ease-out, visibility 0.8s;
        }
        
        .loader-bar {
            width: 150px;
            height: 2px;
            background: rgba(255,255,255,0.1);
            position: relative;
            overflow: hidden;
            margin-top: 20px;
        }
        .loader-progress {
            position: absolute;
            height: 100%;
            width: 50%;
            background: #f59e0b; /* Warna Amber */
            animation: loading 1.5s infinite ease-in-out;
        }
        @keyframes loading {
            from { left: -50%; }
            to { left: 100%; }
        }
    </style>
</head>
<body class="bg-[#050505] text-white flex items-center justify-center min-h-screen p-4 py-10 overflow-hidden">

    <div id="splash">
        <div class="text-5xl mb-4 animate-bounce">🔑</div>
        <h2 class="text-sm font-black tracking-[0.4em] uppercase text-amber-500">Security System</h2>
        <div class="loader-bar">
            <div class="loader-progress"></div>
        </div>
        <p class="text-[9px] text-gray-600 mt-4 italic font-mono uppercase">"Kunci pengetahuan adalah bertanya"</p>
    </div>

    <div class="animate-pop bg-white/5 border border-white/10 p-8 md:p-12 rounded-[40px] w-full max-w-[450px] text-center shadow-2xl relative z-10">
        
        <div class="text-4xl mb-4 drop-shadow-[0_0_15px_rgba(245,158,11,0.5)]">🔑</div>
        
        <h1 class="text-2xl md:text-3xl font-black text-amber-500 mb-2 uppercase tracking-tighter">Pulihkan Akses</h1>
        <p class="text-gray-500 text-[10px] mb-8 font-bold uppercase tracking-widest leading-relaxed">
            Instruksi pemulihan sistem Global School AI.
        </p>

        <% if (locals.msg) { %>
            <div class="bg-amber-500/10 text-amber-200 p-4 rounded-2xl mb-6 text-center font-bold border border-amber-500/20 text-xs uppercase tracking-wider">
                <%= msg %>
            </div>
        <% } %>
        
        <% if (typeof step === 'undefined' || step === 1) { %>
            <form action="/auth/forget" method="POST" class="space-y-4">
                <div class="group relative">
                    <input name="email" type="email" placeholder="ALAMAT EMAIL ADMIN" 
                        class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-amber-500 text-lg text-center transition-all placeholder:text-gray-700 font-bold" 
                        required>
                </div>

                <button type="submit" 
                    class="w-full bg-amber-600 py-5 rounded-2xl font-black text-xl hover:bg-amber-500 transition-all shadow-lg shadow-amber-600/20 uppercase mt-2 active:scale-95">
                    Kirim OTP Bantuan
                </button>
            </form>

        <% } else { %>
            <form action="/auth/reset-password" method="POST" class="space-y-4">
                <input type="hidden" name="email" value="<%= email %>">
                
                <div class="space-y-2">
                    <label class="text-[10px] font-bold text-gray-500 uppercase tracking-[0.2em]">6-Digit Kode OTP</label>
                    <input name="otp" type="text" placeholder="000000" maxlength="6"
                        class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-amber-500 text-3xl text-center font-black tracking-[0.3em] transition-all placeholder:text-gray-800" 
                        required>
                </div>

                <div class="space-y-2">
                    <label class="text-[10px] font-bold text-gray-500 uppercase tracking-[0.2em]">Password Baru</label>
                    <input name="newPass" type="password" placeholder="••••••••" 
                        class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-amber-500 text-lg text-center transition-all placeholder:text-gray-800" 
                        required>
                </div>

                <button type="submit" 
                    class="w-full bg-green-600 py-5 rounded-2xl font-black text-xl hover:bg-green-500 transition-all shadow-lg shadow-green-600/20 uppercase mt-4 active:scale-95 text-white">
                    Simpan Password
                </button>
            </form>
        <% } %>
        
        <div class="mt-10 pt-6 border-t border-white/5">
            <a href="/login" class="text-[10px] text-gray-500 hover:text-amber-500 font-bold uppercase tracking-widest transition-colors flex items-center justify-center gap-2">
                <span>⬅</span> Kembali ke Gerbang Login
            </a>
        </div>
    </div>

    <script>
        window.addEventListener('load', () => {
            setTimeout(() => {
                const splash = document.getElementById('splash');
                splash.style.opacity = '0';
                splash.style.visibility = 'hidden';
                document.body.style.overflow = 'auto';
            }, 2000); // Tampil selama 2 detik
        });
    </script>
</body>
</html>
