<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Animasi Masuk dari Bawah */
        @keyframes slideUp { 
            from { opacity: 0; transform: translateY(40px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        
        /* Animasi Berdenyut Halus */
        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(37, 99, 235, 0.1); }
            50% { box-shadow: 0 0 40px rgba(37, 99, 235, 0.3); }
        }

        /* Efek Kaca (Glassmorphism) */
        .glass { 
            background: rgba(255, 255, 255, 0.03); 
            backdrop-filter: blur(16px); 
            -webkit-backdrop-filter: blur(16px); 
            border: 1px solid rgba(255, 255, 255, 0.1); 
        }
        
        .animate-enter { animation: slideUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; }
    </style>
</head>
<body class="bg-[#050505] text-white flex items-center justify-center min-h-screen bg-[url('https://source.unsplash.com/random/1920x1080/?futuristic,technology,dark')] bg-cover bg-no-repeat bg-blend-overlay bg-black/80">
    
    <div class="glass p-10 rounded-[40px] w-96 shadow-2xl animate-enter relative overflow-hidden">
        
        <div class="absolute top-0 left-1/2 -translate-x-1/2 w-32 h-32 bg-blue-600/20 rounded-full blur-[80px] -z-10"></div>

        <div class="text-center mb-8">
            <h1 class="text-4xl font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-blue-500 to-purple-600 italic tracking-tighter">GLOBAL<br>SCHOOL</h1>
            <p class="text-[10px] text-gray-400 tracking-[0.4em] mt-2 uppercase font-mono">AI Learning System</p>
        </div>

        <% if (msg) { %>
            <div class="bg-red-500/10 border border-red-500/30 text-red-200 p-3 rounded-2xl mb-6 text-xs text-center font-bold animate-pulse">
                ⚠️ <%= msg %>
            </div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-5">
            <div class="group relative">
                <input name="kode" placeholder="KODE INSTANSI" class="w-full bg-black/40 border border-white/10 p-4 rounded-2xl outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-all text-center tracking-widest font-mono text-blue-300 placeholder-gray-600" required>
            </div>
            
            <div class="group">
                <input name="email" type="email" placeholder="EMAIL ADMIN" class="w-full bg-black/40 border border-white/10 p-4 rounded-2xl outline-none focus:border-blue-500 focus:bg-blue-500/5 transition-all placeholder-gray-600" required>
            </div>
            
            <div class="group">
                <input name="pass" type="password" placeholder="PASSWORD" class="w-full bg-black/40 border border-white/10 p-4 rounded-2xl outline-none focus:border-blue-500 focus:bg-blue-500/5 transition-all placeholder-gray-600" required>
            </div>
            
            <button class="w-full bg-gradient-to-r from-blue-600 to-indigo-700 py-4 rounded-2xl font-black text-lg hover:scale-[1.02] hover:shadow-lg hover:shadow-blue-500/30 transition-all duration-300 uppercase tracking-wide">
                Masuk System
            </button>
        </form>

        <div class="mt-8 flex justify-between items-center text-[10px] font-bold text-gray-500 tracking-widest uppercase border-t border-white/5 pt-6">
            <a href="/register" class="hover:text-blue-400 transition-colors flex items-center gap-1">
                <span>➕</span> Daftar Baru
            </a>
            <a href="/forget" class="hover:text-blue-400 transition-colors">
                Lupa Kode?
            </a>
        </div>
    </div>
</body>
</html>
