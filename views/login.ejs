<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Guru - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#050505] text-white flex items-center justify-center min-h-screen p-4 py-10 overflow-y-auto">
    <div class="bg-white/5 border border-white/10 p-8 md:p-12 rounded-[40px] w-full max-w-[450px] shadow-2xl">
        <h1 class="text-3xl font-black text-blue-500 mb-8 tracking-tighter uppercase">Login Guru</h1>

        <% if (locals.msg) { %>
            <div class="bg-red-500/20 text-red-200 p-4 rounded-xl mb-6 text-center font-bold border border-red-500/30 text-sm">
                <%= msg %>
            </div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-4">
            <div class="space-y-1">
                <label class="text-[10px] font-bold text-gray-500 ml-2 uppercase">Kode Sekolah</label>
                <input name="kode" placeholder="SCH-XXXXX" class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-blue-500 text-lg font-mono text-blue-400" required>
            </div>
            <div class="space-y-1">
                <label class="text-[10px] font-bold text-gray-500 ml-2 uppercase">Email Guru</label>
                <input name="email" type="email" placeholder="nama@sekolah.com" class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-blue-500 text-lg" required>
            </div>
            <div class="space-y-1">
                <label class="text-[10px] font-bold text-gray-500 ml-2 uppercase">Password</label>
                <input name="pass" type="password" placeholder="••••••••" class="w-full bg-black/50 border border-white/10 p-5 rounded-2xl outline-none focus:border-blue-500 text-lg" required>
            </div>
            <button class="w-full bg-blue-600 py-5 rounded-2xl font-black text-xl hover:bg-blue-500 transition-all shadow-lg shadow-blue-600/30 mt-4 uppercase">Masuk Sistem</button>
        </form>

        <div class="mt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-[10px] font-bold text-gray-500 tracking-widest uppercase">
            <a href="/register" class="hover:text-blue-400">Daftar Instansi</a>
            <a href="/forget" class="hover:text-blue-400">Lupa Kode / Password?</a>
        </div>
    </div>
</body>
</html>
