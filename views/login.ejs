<!DOCTYPE html>
<html>
<head><script src="https://cdn.tailwindcss.com"></script></head>
<body class="bg-[#050505] text-white flex items-center justify-center min-h-screen">
    <div class="bg-white/5 border border-white/10 p-10 rounded-[40px] w-96 shadow-2xl">
        <h1 class="text-3xl font-black text-center text-blue-500 mb-6 italic">GLOBAL SCHOOL</h1>
        <% if (msg) { %><p class="bg-blue-600/20 text-blue-200 p-3 rounded-xl mb-4 border border-blue-500/50 text-center text-xs"><%= msg %></p><% } %>
        <form action="/auth/login" method="POST" class="space-y-4">
            <input name="kode" placeholder="KODE INSTANSI" class="w-full bg-black/50 border border-white/10 p-4 rounded-2xl outline-none focus:border-blue-500" required>
            <input name="email" type="email" placeholder="EMAIL ADMIN" class="w-full bg-black/50 border border-white/10 p-4 rounded-2xl outline-none" required>
            <input name="pass" type="password" placeholder="PASSWORD" class="w-full bg-black/50 border border-white/10 p-4 rounded-2xl outline-none" required>
            <button class="w-full bg-blue-600 py-4 rounded-2xl font-black text-lg hover:bg-blue-500 transition-all">MASUK</button>
        </form>
        <div class="mt-6 flex justify-between text-[10px] font-bold text-gray-500 tracking-widest uppercase">
            <a href="/register" class="hover:text-blue-400">Daftar Baru</a>
            <a href="/forget" class="hover:text-blue-400">Lupa Kode?</a>
        </div>
    </div>
</body>
</html>
