<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Guru - Global School AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes popIn {
            from { opacity: 0; transform: scale(0.95) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        .animate-pop { animation: popIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }

        /* Splash Screen Putih */
        #splash {
            position: fixed; inset: 0; background: #ffffff; z-index: 100;
            display: flex; flex-direction: column; align-items: center; justify-content: center;
            transition: opacity 0.8s ease-out, visibility 0.8s;
        }
        .loader-bar { width: 150px; height: 3px; background: #e2e8f0; position: relative; overflow: hidden; margin-top: 20px; border-radius: 10px; }
        .loader-progress { position: absolute; height: 100%; width: 50%; background: #4f46e5; animation: loading 1.5s infinite ease-in-out; border-radius: 10px; }
        @keyframes loading { from { left: -50%; } to { left: 100%; } }
        
        body { background-color: #f1f5f9; }
    </style>
</head>
<body class="text-slate-800 flex items-center justify-center min-h-screen p-4 py-10 overflow-hidden font-sans">

    <div id="splash">
        <div class="text-6xl mb-4 animate-bounce">🏫</div>
        <h2 class="text-sm font-bold tracking-[0.3em] uppercase text-indigo-600">Teacher Portal</h2>
        <div class="loader-bar"><div class="loader-progress"></div></div>
    </div>

    <div class="animate-pop bg-white p-8 md:p-12 rounded-[30px] w-full max-w-[450px] shadow-2xl shadow-indigo-100 border border-slate-100 relative z-10">
        
        <div class="flex justify-between items-start mb-8">
            <div>
                <h1 class="text-3xl font-bold text-slate-800 tracking-tight">Login Guru</h1>
                <p class="text-xs text-slate-400 font-semibold uppercase tracking-wider mt-2">Masuk ke Dashboard Pengajar</p>
            </div>
            <div class="p-3 bg-indigo-50 rounded-xl text-2xl">👨‍🏫</div>
        </div>

        <% if (locals.msg) { %>
            <div class="bg-indigo-50 text-indigo-600 p-4 rounded-xl mb-6 text-center font-bold border border-indigo-100 text-xs shadow-sm">
                <%= msg %>
            </div>
        <% } %>

        <form action="/auth/login-guru" method="POST" class="space-y-5"> 
            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">Email Sekolah</label>
                <input name="email" type="email" placeholder="guru@sekolah.com" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-indigo-500 focus:bg-white focus:ring-2 focus:ring-indigo-100 text-lg transition-all placeholder:text-slate-300 text-slate-700 font-medium" required>
            </div>

            <div class="space-y-1">
                <label class="text-[11px] font-bold text-slate-400 ml-1 uppercase tracking-wider">Password</label>
                <input name="pass" type="password" placeholder="••••••••" 
                    class="w-full bg-slate-50 border border-slate-200 p-4 rounded-xl outline-none focus:border-indigo-500 focus:bg-white focus:ring-2 focus:ring-indigo-100 text-lg transition-all placeholder:text-slate-300 text-slate-700 font-medium" required>
            </div>

            <button type="submit" class="w-full bg-indigo-600 py-4 rounded-xl font-bold text-lg hover:bg-indigo-700 transition-all shadow-lg shadow-indigo-200 mt-2 text-white active:scale-95">
                Masuk Sistem
            </button>
        </form>

        <div class="mt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-[11px] font-bold text-slate-400 tracking-wider uppercase">
            <a href="/register-guru" class="hover:text-indigo-600 transition-colors">Daftar Akun Baru</a>
            <a href="/forget" class="hover:text-indigo-600 transition-colors">Lupa Password?</a>
        </div>

        <div class="mt-8 pt-6 border-t border-slate-100 text-center">
            <a href="/" class="text-[10px] font-bold text-slate-400 hover:text-indigo-600 transition-colors uppercase tracking-widest flex items-center justify-center gap-2">
                <span>⬅</span> Kembali ke Portal
            </a>
        </div>
    </div>

    <script>
        window.addEventListener('load', () => {
            setTimeout(() => {
                const splash = document.getElementById('splash');
                splash.style.opacity = '0';
                splash.style.visibility = 'hidden';
                document.body.style.overflow = 'auto';
            }, 1500);
        });
    </script>
</body>
</html>
